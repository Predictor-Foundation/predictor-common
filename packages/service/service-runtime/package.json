{
	"name": "@predictor-foundation/service-runtime",
	"version": "0.1.1",
	"description": "Long-lived service runtime building blocks (health state machine, k8s probe routes, tick loop, graceful shutdown) for the Predictor Foundation Node services",
	"license": "UNLICENSED",
	"private": false,
	"publishConfig": {
		"registry": "https://npm.pkg.github.com"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/Predictor-Foundation/predictor-common.git",
		"directory": "packages/service/service-runtime"
	},
	"main": "lib/index.js",
	"types": "lib/index.d.ts",
	"files": [
		"lib/",
		"README.md"
	],
	"engines": {
		"node": ">=20"
	},
	"scripts": {
		"build": "rm -rf lib *.tsbuildinfo && tsc",
		"typecheck": "tsc --noEmit",
		"clean": "rm -rf lib *.tsbuildinfo"
	},
	"dependencies": {
		"@predictor-foundation/logger": "workspace:*"
	},
	"peerDependencies": {
		"fastify": "^5.0.0"
	},
	"devDependencies": {
		"@types/node": "catalog:",
		"fastify": "^5.8.5",
		"typescript": "catalog:"
	}
}
