import { CircularProgress } from "@mui/material";

/** A large theme-coloured loading spinner (uses the theme's primary/lime accent). */
export const Spinner = () => <CircularProgress color="primary" size={54} thickness={6} />;
