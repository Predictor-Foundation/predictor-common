{
	"name": "@predictor-foundation/network-constants",
	"version": "0.1.1",
	"description": "Single source of truth for Predictor network coordinates: SS58 prefix, token decimals/symbol, WS endpoints, explorer base URLs and deep-link builders",
	"license": "UNLICENSED",
	"private": false,
	"publishConfig": {
		"registry": "https://npm.pkg.github.com"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/Predictor-Foundation/predictor-common.git",
		"directory": "packages/chain/network-constants"
	},
	"main": "lib/index.js",
	"types": "lib/index.d.ts",
	"files": [
		"lib/",
		"README.md"
	],
	"engines": {
		"node": ">=20"
	},
	"scripts": {
		"build": "rm -rf lib *.tsbuildinfo && tsc",
		"typecheck": "tsc --noEmit",
		"clean": "rm -rf lib *.tsbuildinfo"
	},
	"devDependencies": {
		"@types/node": "^22.0.0",
		"typescript": "^5.6.0"
	}
}
